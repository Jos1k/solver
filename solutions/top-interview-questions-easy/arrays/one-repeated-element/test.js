const repeatedElement = require("./submissions/submission_1/code");

const inputArray = [1,5,6,8,7,10,12,15,12]; //Put any other array or write func for auto generate array
console.log(`input array: ${inputArray}`);

const result = repeatedElement(inputArray);
console.log(`repeated element: ${result}`);